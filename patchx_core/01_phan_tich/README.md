# 01 — PHÂN TÍCH

Nhóm này sẽ chứa code đọc APK và tạo `app_model.json`.

Hiện tại code thật vẫn ở `patchx_core/` để giữ nguyên import. Đây là mốc phân loại trước khi di chuyển.

Module chính: `model.py`, `smali_lib.py`, `smali_sem.py`.
